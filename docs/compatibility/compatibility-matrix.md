<!-- Generated by wolfppt-harness matrix. Do not edit by hand. -->

# WolfPPT Compatibility Matrix

This matrix is generated from `docs/compatibility/_compat_spec.py`.

## OPC package + preservation

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `package.open.basic` | P0 | [+] Supported | Open a .pptx package and enumerate parts | zip package manifest | Harness extractor records every package part. |
| `package.relationships` | P0 | [+] Supported | Read package and part relationships | relationship manifest | Harness extractor captures source, relationship id, type, target, and target mode. |
| `package.roundtrip_parts` | P0 | [+] Supported | Round-trip packages without part payload loss | semantic diff + package diff | Rust CLI and native round-trip lanes compare every part payload across the fixture corpus. |
| `package.mixed_workload_preservation` | P1 | [+] Supported | Preserve mixed real-world-style decks with text, media, tables, charts, and notes | semantic diff + package diff + benchmark profile | The checked-in mixed workload deck combines five slides with text, picture media, a table, a chart with embedded workbook, and speaker notes for core benchmark and corpus lanes. |
| `package.macro_preservation` | P0 | [+] Supported | Detect and preserve macro-enabled .pptm packages | package manifest + content types | Rust CLI and native round-trip lanes preserve the checked-in .pptm VBA package fixture with clean package diff. |
| `package.ole_preservation` | P1 | [+] Supported | Detect and preserve slide-level OLE object relationships and embedded binaries | relationship manifest + package diff | Checked-in OLE fixture carries a slide-level oleObject relationship plus an embedded binary package; Rust CLI/native round-trip lanes preserve both with clean package diffs. |

## Presentation + slides

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `presentation.slide_order` | P0 | [+] Supported | Read slide order and slide part names | semantic extractor | Fallback natural sort is used when relationship ordering is incomplete. |
| `presentation.slide_add_from_layout` | P0 | [+] Supported | Add a slide from an existing layout | PowerPoint repair check + package diff | Rust CLI/native slide append can use the first slide's layout or a selected layout index, materializes editable placeholders without layout prompt text, and validates through Open XML SDK. |
| `presentation.slide_reorder` | P0 | [+] Supported | Reorder existing slides without changing their parts or relationships | python-pptx behavior probe + package diff + Open XML validation | SlideCollection.move reorders p:sldId entries in the native edit batch while preserving slide ids, relationship ids, slide part names, and every non-presentation part payload. |
| `presentation.slide_duplicate` | P0 | [+] Supported | Duplicate an existing slide while preserving its owned relationships | semantic extractor + package diff | SlideCollection.duplicate appends a new slide part copied verbatim from the source, shares the source layout and image targets, and fails explicitly on relationship types without a copy policy. |
| `presentation.slide_duplicate_chart` | P0 | [+] Supported | Duplicate a slide that owns chart relationships including embedded workbooks | semantic extractor + package diff + Open XML validation | Chart parts, colors/style parts, and embedded xlsx workbooks are deep-copied to new part names with content types registered; the duplicated slide's relationship keeps the source r:id, so series edits on the duplicate never alias the source chart. |
| `presentation.slide_duplicate_notes` | P1 | [+] Supported | Duplicate a slide together with its notes slide | semantic extractor + package diff | The notesSlide part and its relationships are copied to a new part name; the copied notes slide's back-reference to the source slide is rewritten to the duplicate, and the notes master relationship stays shared. |
| `presentation.slide_duplicate_ole` | P1 | [+] Supported | Duplicate a slide that owns OLE object relationships and embedded binaries | relationship manifest + package diff | OLE relationship targets and their embedded binary packages are copied to new part names with the bin default content type registered, so the duplicate owns an independent embedded object. |
| `presentation.slide_duplicate_comments` | P2 | [+] Supported | Duplicate a slide that owns slide comments | relationship manifest + package diff | The slide-scoped comments part is copied to a new part name with its content type override registered; the presentation-scoped comment author part stays shared. |
| `presentation.slide_delete` | P0 | [+] Supported | Delete an existing slide and clean up owned package parts and relationships | python-pptx behavior probe + package diff + Open XML validation | Slide deletion removes the slide from presentation.xml and presentation.xml.rels, deletes the unreferenced slide part and slide relationships, and updates [Content_Types].xml. |
| `presentation.pending_slide_delete` | P1 | [+] Supported | Delete pending newly-added or duplicated slides before save | python-pptx behavior probe + package diff + Open XML validation | SlideCollection.remove and del prs.slides[i] delete pending slides added via add_slide() or duplicate() before save, matching by creation identity token, unwinding slide_creations and slide_order queues, rebasing survivor indices, and ensuring zero package delta relative to an untouched save. Pending shape cancellation remains unsupported. |
| `presentation.slide_slice_delete` | P2 | [+] Supported | Delete slides by slice across persisted and pending slides | python-pptx behavior probe + package diff + Open XML validation | wolfppt API completion beyond python-pptx (python-pptx raises TypeError on slice deletion); composes with persisted + pending removal via sequential remove(); all-slides deletion rejected before mutation; empty slice no-op; step + negative bounds supported. |
| `presentation.slide_duplicate_copy_policies` | P1 | [+] Supported | Duplicate slides owning unhandled relationship types via explicit copy policies | relationship manifest + package diff + Open XML validation | audio, video, and media relationships share immutable media assets under ppt/media/ with unchanged rIds and targets, covered by [Content_Types].xml Defaults; comments relationships are copied to a new owned ppt/comments/commentN.xml with Content-Type Override and remapped rel target while comment authors remain shared presentation-level assets; external and unhandled relationship types continue to fail fast with an explicit copy-policy error. |
| `presentation.slide_duplicate_external_targets` | P1 | [+] Supported | Duplicate slides owning external-target relationships (e.g. hyperlinks) | python-pptx behavior probe + semantic diff + package diff | external-target relationships (e.g. hyperlinks) pass through verbatim into the duplicate slide's rels — same rId, Type, Target, TargetMode; no package parts are created; unknown internal types still rejected; python-pptx element-level equivalence tests are the evidence. |

## Shapes + DrawingML

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `shapes.iterate` | P0 | [+] Supported | Iterate slide shape tree with ids, names, z-order, and transforms | semantic extractor | Python semantic oracles and Rust typed summaries expose the supported shape tree, including ids, names, kinds, text, tables, relationships, transforms, and children. |
| `shapes.delete` | P0 | [+] Supported | Delete an existing shape without disturbing unrelated slide content | python-pptx behavior probe + package diff + Open XML validation | ShapeCollection.remove deletes one top-level shape in the native edit batch, updates live collections, and removes relationships owned only by that shape while preserving related package parts. |
| `shapes.group_child_delete` | P1 | [+] Supported | Delete a child shape inside a group shape without disturbing unrelated content | python-pptx behavior probe + package diff + Open XML validation | Group child deletion removes the shape element from the group shape tree while preserving sibling shapes and group transform bounds. |
| `shapes.nested_group_child_delete` | P1 | [+] Supported | Delete a child shape inside a nested group shape without disturbing unrelated content | python-pptx behavior probe + package diff + Open XML validation | GroupShapeCollection.remove supports any-depth child deletion and whole sub-group subtree removal with ownership-closure part cleanup, while retaining rejection of unsaved or pending targets. |
| `shapes.grouped` | P1 | [+] Supported | Read grouped shapes and nested transforms | semantic extractor + Rust typed summary + render diff | Python semantic oracles and Rust typed summaries model group nodes, child shapes, local transforms, and inherited effective transforms with a checked-in grouped-shape fixture. |
| `shapes.style_color` | P1 | [+] Supported | Set solid, gradient, line RGB color, and line width on shape elements | python-pptx parity + package diff + Open XML validation | The Python facade supports shape.fill.solid(), shape.fill.gradient(), gradient angle/stop edits, shape.fill.fore_color.rgb, shape.line.color.rgb, and shape.line.width for existing shape elements, including direct children of top-level group shapes, while only changing the target slide XML. |

## Text

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `text.extract` | P0 | [+] Supported | Extract text from shapes, paragraphs, and runs | semantic extractor |  |
| `text.replace_preserve_formatting` | P0 | [+] Supported | Replace text while preserving shape and run formatting | semantic diff + package diff | Exact-text, selected-run, and selected-paragraph operations preserve surrounding XML; shape-level text setting rewrites the target text body into newline-separated paragraphs while preserving unrelated package parts. |

## Tables

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `tables.row_column_mutation` | P1 | [+] Supported | Insert and delete table rows and columns | python-pptx behavior probe + semantic diff + package diff | table.rows and table.columns collections expose insert(index) and remove(index) through the native edit batch; inserted rows and columns carry empty cell text, merged-cell and sole-row/column deletions are rejected explicitly, and only the owning slide part changes. |
| `tables.extract` | P1 | [+] Supported | Extract table rows, cells, merges, and cell text | semantic extractor |  |
| `tables.write` | P1 | [+] Supported | Create and edit basic tables | semantic diff + render diff | Rust CLI/native table operations can add basic empty tables with explicit EMU bounds and replace existing table cell text by slide/table/row/column index while preserving unrelated package parts. |

## Charts

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `charts.detect` | P1 | [+] Supported | Detect charts and chart relationships | relationship manifest | Python and Rust semantic summaries bucket slide chart relationships. |
| `charts.data_roundtrip` | P1 | [+] Supported | Preserve chart XML and embedded workbook data | package diff + semantic extractor | Checked-in chart fixture includes chart XML plus embedded workbook; Rust CLI/native round-trip lanes preserve both with clean package diff. |
| `charts.data_edit` | P1 | [+] Supported | Replace simple category, XY, and bubble chart data | python-pptx parity + package diff + Open XML validation | The Python facade supports python-pptx-style CategoryChartData, XyChartData, and BubbleChartData replacement, including adding or removing series, date-category data, sparse/mismatched and hierarchical category chart data, and uneven XY/bubble point counts by cloning/removing chart series XML and updating the embedded workbook while only changing those two package parts. No-series replacement mirrors python-pptx parity, but zero-plot outputs can fail strict Open XML validation, so claim-grade benchmark lanes use Open XML-valid empty-series and sparse cases. |

## Images + media

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `media.images` | P0 | [+] Supported | Read image relationships and picture shapes | semantic extractor | Python and Rust semantic summaries bucket slide image relationships. |
| `media.add_replace_image` | P0 | [+] Supported | Add or replace pictures while preserving relationships | package diff + PowerPoint repair check | Rust CLI/native image operations can replace existing image payloads globally or by slide index and add new PNG/JPEG/GIF/BMP/TIFF picture shapes with explicit EMU bounds, python-pptx-style JPEG/TIFF part-extension canonicalization, and unrelated package-part preservation. |
| `media.add_movie` | P1 | [+] Supported | Add movie media parts with poster frames and slide relationships | python-pptx parity probe + package diff + Open XML validation | The native writer adds a video media part, PNG/JPEG/GIF/BMP/TIFF poster image part, media/video/image slide relationships, content-type defaults, movie picture XML, and timing markers while preserving unrelated package parts. |
| `media.add_ole_object` | P1 | [+] Supported | Add embedded OLE object parts with icon images and slide relationships | python-pptx parity probe + package diff + Open XML validation | The native writer adds an OLE embedding part, PNG/JPEG/GIF/BMP/TIFF icon image part, oleObject/image slide relationships, an OLE content-type override, and graphic-frame XML while preserving unrelated package parts. |

## Notes + comments + review parts

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `side_parts.notes` | P1 | [+] Supported | Extract, create, and edit speaker notes text, text-run and placeholder click hyperlinks, paragraph/run font formatting, paragraph layout, and notes text-frame properties | python-pptx parity + package diff | The Python facade reads speaker notes, creates missing notes-slide package parts on first notes access, and can edit notes text-run hyperlinks, placeholder click hyperlinks, notes text, paragraph/run content, paragraph and run font formatting, paragraph alignment, level, spacing, placeholder clones, margins, word-wrap, vertical anchor, and auto-size while preserving visible slide content. |
| `side_parts.comments` | P2 | [+] Supported | Preserve comments and review metadata | package diff | Checked-in legacy comment fixture validates cleanly; Python and Rust semantic summaries bucket slide comment relationships and Rust round-trip lanes preserve comment/comment-author parts with clean package diffs. |

## Transitions + animations

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `effects.transitions` | P2 | [+] Supported | Preserve slide transitions | package diff | Checked-in transition fixture validates cleanly; Python and Rust semantic summaries detect transition XML and Rust round-trip lanes preserve it with clean package diffs. |
| `effects.animations` | P2 | [+] Supported | Preserve animation timing trees | package diff | Checked-in timing fixture validates cleanly; Python and Rust semantic summaries detect timing XML and Rust round-trip lanes preserve it with clean package diffs. |

## Python-compatible API

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `python_api.presentation_open_save` | P0 | [+] Supported | Create blank presentations, open existing decks from paths or readable file-like objects, iterate slides and shapes, and save to paths or writable file-like outputs | unit tests + python-pptx parity probe + package diff | The public facade exposes python-pptx-style slide/shape collections while preserving packages on no-op saves, supporting BytesIO-style open/save targets, and constructing blank presentations from a packaged template without importing python-pptx. |
| `python_api.part_proxy` | P0 | [+] Supported | Expose python-pptx-style .part metadata for presentation, slide, layout, shape, action, text, chart, table, and common collection objects | python-pptx parity probe + package blob check | The facade exposes read-only package part metadata with partname, content_type, blob, part, package, and name behavior for common python-pptx objects without adding a mutation path. |
| `python_api.xml_element_inspection` | P0 | [+] Supported | Expose python-pptx-style read-only .element XML inspection for presentation, slide, layout, shape, placeholder, and master objects | python-pptx parity probe | The facade exposes tag, attrib, text, xml, find, findall, xpath, iteration, indexing, and length on copied XML elements for inspection without adding arbitrary XML mutation persistence. |
| `python_api.presentation_slide_size` | P0 | [+] Supported | Read and set Presentation.slide_width and Presentation.slide_height | python-pptx parity probe + package diff + XML size check | The facade mirrors python-pptx slide size reads and EMU range validation, and saves size edits by changing only the presentation XML part. |
| `python_api.core_properties` | P0 | [+] Supported | Read and set Presentation.core_properties common document metadata | python-pptx parity probe + package diff + XML metadata check | The facade mirrors python-pptx for common core properties including title, subject, author, keywords, comments, revision, timestamps, and status metadata while changing only docProps/core.xml. |
| `python_api.slide_metadata` | P0 | [+] Supported | Read slide.slide_id, slide.slide_layout, slide.follow_master_background, slide.has_notes_slide, use slides.get(), and set slide.name | python-pptx parity probe + package diff + XML name check | The facade resolves slide ids and layouts from package relationships, detects notes-slide relationships and inherited backgrounds, and saves slide.name edits by changing only the target slide XML part. |
| `python_api.notes_slide_read` | P0 | [+] Supported | Read existing or create missing slide.notes_slide, notes_text_frame, notes shapes/placeholders, and Presentation.notes_master | python-pptx parity probe + package part check + drop-in benchmark | The facade mirrors python-pptx read access for packages that already contain notes parts and creates missing notes-slide, notes-master, relationship, content-type, and theme parts on first notes access while preserving visible slide text. |
| `python_api.notes_text_setter` | P0 | [+] Supported | Set existing notes text, paragraph layout/font, and run formatting via notes_text_frame, paragraph, and run APIs | python-pptx parity probe + package diff + drop-in benchmarks | Notes text assignment, content-level paragraph/run edits, run font formatting, text-run hyperlinks, paragraph default-font formatting, and paragraph alignment/level/spacing route through a part-scoped edit queue and change only the target notesSlide XML part and notesSlide relationships when hyperlinks change. |
| `python_api.background_fill` | P0 | [+] Supported | Read slide, layout, slide-master, and notes-master background elements and fill state; set slide, layout, and slide-master background solid/background fills and foreground colors | python-pptx parity probe + package diff + drop-in benchmark + Open XML validation | The facade mirrors python-pptx background.element, _element, fill.type, foreground RGB/theme-color/brightness reads, and routes slide, layout, and slide-master background fill.solid(), fill.background(), fill.fore_color.rgb, fill.fore_color.theme_color, and fill.fore_color.brightness through part-scoped edits that change only the target XML part. |
| `python_api.slide_master_metadata` | P0 | [+] Supported | Read Presentation.slide_master, Presentation.slide_masters, master slide_layouts, shapes, and placeholders | python-pptx parity probe + package part check | The facade resolves slide masters and their layouts from package relationships and exposes read-only master placeholder metadata for template inspection workflows. |
| `python_api.placeholder_title_access` | P0 | [+] Supported | Read slide.shapes.title, slide.placeholders, and placeholder_format metadata | python-pptx parity probe + native summary + semantic diff + package diff | The Rust summary exposes placeholder metadata and the facade resolves title placeholders while routing title text assignment through the native shape-text operation. |
| `python_api.shape_collection_helpers` | P0 | [+] Supported | Read slide.shapes.placeholders, shape-tree ph_basename(), and turbo_add_enabled | python-pptx parity probe | The facade mirrors python-pptx shape collection convenience helpers without making turbo_add_enabled affect preservation-oriented id allocation. |
| `python_api.clone_layout_placeholders` | P0 | [+] Supported | Clone editable layout placeholders with slide.shapes.clone_layout_placeholders(...) and clone_placeholder(...) | python-pptx parity probe + package diff + native placeholder insertion | The facade exposes layout shapes/placeholders, skips latent date/footer/slide-number placeholders, and queues native placeholder insertion that changes only the target slide XML. |
| `python_api.shape_text_setter` | P0 | [+] Supported | Set shape.text and save through the native Rust mutation engine | python-pptx parity probe + semantic diff + package diff | Shape text assignment queues an edit and save applies it through the native set-shape-text operation with a parity probe against python-pptx, including existing direct children of top-level group shapes. |
| `python_api.text_frame_clear` | P0 | [+] Supported | Clear shape.text_frame while preserving python-pptx empty-paragraph semantics | python-pptx parity probe + semantic diff + package diff | TextFrame.clear() queues a native shape-text edit and keeps one empty paragraph available in memory for follow-up edits before save. |
| `python_api.shape_geometry` | P0 | [+] Supported | Read and set shape.left, shape.top, shape.width, and shape.height | python-pptx parity probe + semantic diff + package diff | The facade exposes EMU geometry properties and coalesces geometry setters into native Rust geometry batch edits, including existing direct children of top-level group shapes. |
| `python_api.shape_name` | P0 | [+] Supported | Read and set shape.name | python-pptx parity probe + XML name check + package diff + Open XML validation | The facade mirrors python-pptx shape name assignment and patches the target non-visual shape properties while changing only the target slide XML, including existing direct children of top-level group shapes. |
| `python_api.shape_type` | P0 | [+] Supported | Read shape.shape_type for common shape classes | python-pptx parity probe | The facade mirrors python-pptx's TEXT_BOX vs AUTO_SHAPE distinction for DrawingML shape elements by honoring p:cNvSpPr txBox="1". |
| `python_api.picture_image_inspection` | P0 | [+] Supported | Read shape.image blob, content type, extension, filename, SHA1, pixel size, and DPI for picture shapes | python-pptx parity probe + package blob check | The facade resolves picture blip relationships to image package parts and exposes an immutable python-pptx-style image value object while non-picture shapes raise AttributeError. |
| `python_api.picture_image_replacement` | P0 | [+] Supported | Replace an existing picture image payload while preserving placement and relationships | package blob check + relationship preservation check | The facade exposes shape.replace_image(...) for saved picture shapes and queues native relationship-preserving media-part replacement so slide XML, geometry, and relationships remain unchanged. |
| `python_api.shape_rotation` | P0 | [+] Supported | Read and set shape.rotation | python-pptx parity probe + XML rotation check + package diff + Open XML validation | The facade mirrors python-pptx clockwise rotation normalization and patches the target shape transform while changing only the target slide XML, including existing direct children of top-level group shapes. |
| `python_api.shape_style_color` | P0 | [+] Supported | Set shape.fill.solid(), shape.fill.background(), shape.fill.gradient(), shape.fill.gradient_angle, shape.fill.gradient_stops, shape.fill.patterned(), shape.fill.pattern, shape.fill.fore_color.rgb, shape.fill.back_color.rgb, shape.line.fill.solid(), shape.line.fill.background(), shape.line.fill.patterned(), shape.line.fill.pattern, shape.line.fill.fore_color.rgb, shape.line.fill.back_color.rgb, shape.line.color.rgb, shape.line.width, and shape.line.dash_style | python-pptx parity probe + package diff + Open XML validation | The facade exposes a python-pptx-style fill and line surface for shape and line solid/background/gradient/patterned fill modes, gradient angle and stop position/RGB edits, preset fill patterns, patterned foreground/background colors, solid RGB styling, line width, and preset line dash styles on existing shape elements, including direct children of top-level group shapes. |
| `python_api.shape_shadow` | P0 | [+] Supported | Read and set shape.shadow.inherit | python-pptx parity probe + XML effectLst check + package diff + Open XML validation | The facade mirrors python-pptx shadow inheritance toggling by adding or removing the target shape's DrawingML effect override while changing only the target slide XML, including existing direct children of top-level group shapes. |
| `python_api.shape_click_hyperlink` | P0 | [+] Supported | Read and set shape.click_action.hyperlink.address for external hyperlinks | python-pptx parity probe + part relationship check + package diff + Open XML validation | The facade mirrors python-pptx for adding and clearing external shape hyperlinks by editing the target slide XML and slide relationship part only, including existing direct children of top-level group shapes and notes-slide placeholders through part-scoped notesSlide relationships. |
| `python_api.shape_click_target_slide` | P0 | [+] Supported | Read and set shape.click_action.target_slide for internal slide jumps | python-pptx parity probe + slide relationship check + package diff + Open XML validation | The facade mirrors python-pptx for adding and clearing named-slide click actions by editing the target slide XML and slide relationship part only, including existing direct children of top-level group shapes. |
| `python_api.text_frame_paragraph_text` | P0 | [+] Supported | Set shape.text_frame.paragraphs[index].text | python-pptx parity probe + semantic diff + package diff | The facade exposes paragraph objects and queues paragraph text edits through native Rust paragraph operations, including existing direct children of top-level group shapes. |
| `python_api.text_paragraph_clear` | P0 | [+] Supported | Clear shape.text_frame.paragraphs[index] while preserving paragraph properties | python-pptx parity probe + XML paragraph check + package diff + Open XML validation | The facade mirrors python-pptx Paragraph.clear() by removing runs, line breaks, and fields while preserving paragraph properties and empty paragraph positions, including existing direct children of top-level group shapes. |
| `python_api.text_paragraph_line_break` | P0 | [+] Supported | Append a line break with shape.text_frame.paragraphs[index].add_line_break() | python-pptx parity probe + XML line-break check + package diff + Open XML validation | The facade mirrors python-pptx by inserting DrawingML br elements, exposing paragraph text with vertical-tab line breaks, and keeping run collections limited to real text runs, including existing direct children of top-level group shapes. |
| `python_api.text_paragraph_font` | P0 | [+] Supported | Read and set paragraph.font default run properties | python-pptx parity probe + XML defRPr check + package diff + Open XML validation | The facade mirrors python-pptx paragraph.font for bold, italic, underline, size, name, RGB/theme color, solid/background/patterned/gradient fill, and language_id by patching paragraph default run properties, including existing direct children of top-level group shapes and speaker-notes text frames. |
| `python_api.text_frame_margins` | P0 | [+] Supported | Read and set shape.text_frame margin_left/right/top/bottom | python-pptx parity probe + XML bodyPr margin check + package diff + Open XML validation | The facade mirrors python-pptx text-frame margin defaults and patches the target text-body properties while changing only the target slide XML, including existing direct children of top-level group shapes. |
| `python_api.text_frame_word_wrap` | P0 | [+] Supported | Read and set shape.text_frame.word_wrap | python-pptx parity probe + XML bodyPr wrap check + package diff + Open XML validation | The facade mirrors python-pptx word-wrap True/False/None behavior and patches the target text-body properties while changing only the target slide XML, including existing direct children of top-level group shapes. |
| `python_api.text_frame_vertical_anchor` | P0 | [+] Supported | Read and set shape.text_frame.vertical_anchor | python-pptx parity probe + XML bodyPr anchor check + package diff + Open XML validation | The facade mirrors python-pptx vertical-anchor enum behavior and patches the target text-body properties while changing only the target slide XML, including existing direct children of top-level group shapes. |
| `python_api.text_frame_auto_size` | P0 | [+] Supported | Read and set shape.text_frame.auto_size | python-pptx parity probe + XML bodyPr autofit check + package diff + Open XML validation | The facade mirrors python-pptx auto-size enum behavior and patches the target text-body properties while changing only the target slide XML, including existing direct children of top-level group shapes. |
| `python_api.text_frame_fit_text` | P0 | [+] Supported | Fit text with shape.text_frame.fit_text(...) | python-pptx parity probe + XML run-property check + package diff + Open XML validation | The facade mirrors python-pptx fit_text side effects by setting word-wrap on, auto-size off, and applying the selected font family, size, bold, and italic properties to text runs and paragraph-end run properties. |
| `python_api.text_paragraph_alignment` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].alignment | python-pptx parity probe + XML paragraph-property check + package diff + Open XML validation | The facade exposes python-pptx-style paragraph alignment values for existing and appended paragraphs, existing direct children of top-level group shapes, and speaker-notes text frames while changing only the target XML part. |
| `python_api.text_paragraph_level` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].level | python-pptx parity probe + XML paragraph-property check + package diff + Open XML validation | The facade mirrors python-pptx paragraph level values 0 through 8 for existing and appended paragraphs, existing direct children of top-level group shapes, and speaker-notes text frames while changing only the target XML part. |
| `python_api.text_paragraph_spacing` | P0 | [+] Supported | Read and set paragraph space_before, space_after, and line_spacing | python-pptx parity probe + XML paragraph-property check + package diff + Open XML validation | The facade mirrors python-pptx paragraph spacing values, including point spacing and line-spacing multiples, for existing and appended paragraphs, existing direct children of top-level group shapes, and speaker-notes text frames while changing only the target XML part. |
| `python_api.text_frame_run_text` | P0 | [+] Supported | Set shape.text_frame.paragraphs[index].runs[index].text | python-pptx parity probe + semantic diff + package diff | The facade exposes paragraph run collections and queues run text edits through native Rust batched edit operations while preserving surrounding XML, including existing direct children of top-level group shapes. |
| `python_api.text_run_font_bold` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].runs[index].font.bold | python-pptx parity probe + XML run-property check + package diff | The native summary extracts DrawingML bold attributes into the facade, and saves queue native Rust run-property edits for existing, appended, appended-paragraph, and replacement-created runs while preserving unrelated package parts. Existing direct children of top-level group shapes and speaker-notes runs use part-scoped XML edits for bold. |
| `python_api.text_run_font_italic` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].runs[index].font.italic | python-pptx parity probe + XML run-property check + package diff | The native summary extracts DrawingML italic attributes into the facade, and saves queue native Rust run-property edits for existing, appended, appended-paragraph, and replacement-created runs while preserving unrelated package parts. Existing direct children of top-level group shapes and speaker-notes runs use part-scoped XML edits for italic. |
| `python_api.text_run_font_underline` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].runs[index].font.underline | python-pptx parity probe + XML run-property check + package diff | The native summary maps DrawingML underline values into bool/None facade state, and saves queue native Rust run-property edits for existing, appended, appended-paragraph, and replacement-created runs. Existing direct children of top-level group shapes and speaker-notes runs use part-scoped XML edits for underline. |
| `python_api.text_run_font_size` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].runs[index].font.size | python-pptx parity probe + XML run-property check + package diff | The native summary converts DrawingML centipoint sizes to python-pptx-compatible integer values, and saves queue native Rust run-property edits for existing, appended, appended-paragraph, and replacement-created runs. Existing direct children of top-level group shapes and speaker-notes runs use part-scoped XML edits for size. |
| `python_api.text_run_font_name` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].runs[index].font.name | python-pptx parity probe + XML run-property check + package diff | The native summary extracts DrawingML latin typefaces into the facade, and saves queue native Rust run-property edits for existing, appended, appended-paragraph, and replacement-created runs. Existing direct children of top-level group shapes and speaker-notes runs use part-scoped XML edits for font name. |
| `python_api.text_run_font_color` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].runs[index].font.color rgb/theme values | python-pptx parity probe + XML run-property check + package diff + Open XML validation | The facade exposes python-pptx-style run font RGB and theme color for existing, appended, appended-paragraph, replacement-created runs, existing direct children of top-level group shapes, and speaker-notes runs while changing only the target XML part. |
| `python_api.text_run_font_language_id` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].runs[index].font.language_id | python-pptx parity probe + XML run-property check + package diff + Open XML validation | The facade mirrors python-pptx language_id enum/None behavior by setting or removing the DrawingML lang attribute on existing runs, existing direct children of top-level group shapes, and speaker-notes runs while changing only the target XML part. |
| `python_api.text_run_font_fill` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].runs[index].font.fill | python-pptx parity probe + XML run-property check + package diff + Open XML validation | The facade supports python-pptx-style run font fill.solid(), fill.background(), fill.patterned(), fill.gradient(), fill.type, pattern foreground/background RGB, gradient angle/stop edits, and fill.fore_color.rgb on existing runs, existing direct children of top-level group shapes, and speaker-notes runs while changing only the target XML part. |
| `python_api.text_run_hyperlink` | P0 | [+] Supported | Read and set shape.text_frame.paragraphs[index].runs[index].hyperlink.address for external hyperlinks | python-pptx parity probe + part relationship check + package diff + Open XML validation | The facade mirrors python-pptx for adding and clearing external text-run hyperlinks by editing the target slide XML and relationship part only, including existing direct children of top-level group shapes and speaker-notes text runs through part-scoped notesSlide relationships. |
| `python_api.text_frame_add_paragraph_run` | P0 | [+] Supported | Append text with shape.text_frame.add_paragraph() and paragraph.add_run() | python-pptx parity probe + semantic diff + package diff | The facade appends paragraphs and runs through native batched edit operations that preserve existing text body XML; appended paragraphs can receive paragraph and run formatting edits before save. |
| `python_api.slide_add_from_layout` | P0 | [+] Supported | Add a slide from prs.slide_layouts[index] through prs.slides.add_slide(layout) | python-pptx parity probe + semantic diff + package diff | The facade exposes slide_layouts and queues add_slide edits through the native Rust add-slide operation. |
| `python_api.add_picture` | P0 | [+] Supported | Add a picture with slide.shapes.add_picture(...) and group.shapes.add_picture(...) | python-pptx parity probe + semantic diff + package diff | The facade accepts path and file-like PNG, JPEG, GIF, BMP, and TIFF image inputs with explicit, native, or proportionally inferred bounds, normalizes JPEG/TIFF part extensions like python-pptx, and queues slide-level, top-level group, and nested-group image insertion through native Rust image operations. Slide-level, top-level group, and nested-group additions expose python-pptx-style shape_id values before save. |
| `python_api.add_movie` | P1 | [+] Supported | Add a movie with slide.shapes.add_movie(movie, left, top, width, height, poster_frame_image=None, mime_type='video/unknown') | python-pptx parity probe + package diff + Open XML validation | The facade exposes python-pptx-style add_movie for path and file-like movie/poster inputs, including PNG/JPEG/GIF/BMP/TIFF poster frames, returns a MEDIA shape with a python-pptx-style shape_id before save, and queues the native movie writer for save. |
| `python_api.add_ole_object` | P1 | [+] Supported | Add an embedded object with slide.shapes.add_ole_object(...) or group.shapes.add_ole_object(...) | python-pptx parity probe + package diff + Open XML validation | The facade exposes python-pptx-style add_ole_object for path and file-like embedded object/icon inputs, including PNG/JPEG/GIF/BMP/TIFF icons, returns an EMBEDDED_OLE_OBJECT shape with a python-pptx-style shape_id before save at slide, top-level group, and nested-group scope, exposes shape.ole_format read metadata, and queues native OLE insertion. |
| `python_api.add_connector` | P0 | [+] Supported | Add connectors with slide.shapes.add_connector(...) and group.shapes.add_connector(...), read endpoint coordinates, and persist begin_connect/end_connect attachments | python-pptx parity probe + XML connector check + package diff + Open XML validation | The facade supports straight, elbow, and curve connector presets at slide level, inside top-level groups, and inside nested groups through native connector-add operations while changing only the target slide XML. Existing and newly added connectors expose python-pptx-style shape_id, begin_x, begin_y, end_x, and end_y reads before save. Slide-level connector begin_connect/end_connect attachments to saved or newly added shapes, including text boxes and pictures, persist connection metadata and endpoint transforms with python-pptx parity probes. |
| `python_api.connector_line_style` | P0 | [+] Supported | Read and set connector line color, width, and dash style | python-pptx parity probe + XML line-style check + package diff + Open XML validation | The facade applies python-pptx-style line color, width, and dash edits to existing connector elements while changing only the target slide XML. |
| `python_api.picture_crop` | P0 | [+] Supported | Read and set picture crop_left, crop_right, crop_top, and crop_bottom | python-pptx parity probe + XML srcRect check + package diff + Open XML validation | The facade supports python-pptx-style picture crop fractions for existing picture elements while changing only the target slide XML. |
| `python_api.add_textbox` | P0 | [+] Supported | Add a text box with slide.shapes.add_textbox(...) and group.shapes.add_textbox(...) | python-pptx parity probe + semantic diff + package diff | The facade queues slide-level, existing-group, newly created group, and nested-group text box insertion through native Rust operations, exposes python-pptx-style shape_id values before save, and allows immediate text assignment before save. |
| `python_api.add_group_shape` | P1 | [+] Supported | Add group shapes with slide.shapes.add_group_shape(...) or group.shapes.add_group_shape() | python-pptx parity probe + semantic diff + package diff + Open XML validation | The facade queues empty slide-level and nested group-shape insertion through native Rust operations. It also supports python-pptx-style grouping of existing top-level slide shapes with slide.shapes.add_group_shape(existing_shapes), existing direct children with group.shapes.add_group_shape(existing_children), and existing direct children inside nested or deeper nested groups, changing only the target slide XML and preserving the argument order. New groups expose python-pptx-style shape_id values before save, and text boxes, basic preset shapes, connectors, pictures, charts, OLE objects, and freeforms can be added inside nested or deeper nested groups before save. |
| `python_api.build_freeform` | P1 | [+] Supported | Build a freeform shape with slide.shapes.build_freeform(...) or group.shapes.build_freeform(...).convert_to_shape() | python-pptx parity probe + XML custom-geometry check + semantic diff + package diff + Open XML validation | The facade mirrors python-pptx local-coordinate line segments, move-to operations, close operations, origin offsets, and shape_id availability before save, then queues native Rust slide-level, top-level group, nested-group, or deeper nested-group freeform insertion that changes only the target slide XML. |
| `python_api.add_shape_basic` | P0 | [+] Supported | Add common preset shapes with slide.shapes.add_shape(...) and group.shapes.add_shape(...) | python-pptx parity probe + XML preset check + semantic diff + package diff | The facade supports common geometry presets including rectangles, ovals, triangles, diamonds, arrows, chevrons, stars, clouds, and other frequent diagram shapes through native Rust auto-shape operations at slide level, inside top-level group shapes, and inside nested group shapes. |
| `python_api.auto_shape_type` | P0 | [+] Supported | Read shape.auto_shape_type for common preset autoshapes and picture-shape rectangle parity | python-pptx parity probe + XML preset check + package diff + Open XML validation | The facade mirrors python-pptx auto_shape_type for supported preset autoshapes, returns RECTANGLE for picture shapes like python-pptx, and raises for text boxes/non-autoshapes. |
| `python_api.shape_adjustments` | P0 | [+] Supported | Read and set shape.adjustments for supported preset autoshapes | python-pptx parity probe + XML guide check + package diff + benchmark coverage + Open XML validation | The facade mirrors python-pptx adjustment collections for supported preset autoshapes such as rounded rectangles at slide level and inside grouped shapes, writing DrawingML adjustment guides while changing only the target slide XML. |
| `python_api.shape_line_xml_helpers` | P0 | [+] Supported | Read shape.ln and create a line XML element with shape.get_or_add_ln() | python-pptx parity probe + XML line check + package diff + Open XML validation | The facade mirrors python-pptx's low-level line element helpers for shape, picture, connector, and freeform elements and persists get_or_add_ln() by changing only the target slide XML. |
| `python_api.table_cell_text` | P0 | [+] Supported | Read table dimensions, set row/column dimensions, and set table.cell(row, col).text | python-pptx parity probe + semantic diff + package diff + benchmark profile | The facade exposes shape.has_table, shape.table, rows, columns, queued row/column dimension edits, and queued cell text edits through the native Rust table-cell operation. |
| `python_api.table_cell_text_frame` | P0 | [+] Supported | Read and set table cell text_frame text, paragraph formatting, margins, fit, and anchors | python-pptx parity probe + package diff + surface ratchet | The facade mirrors python-pptx table.cell(row, col).text_frame text, paragraphs, add_paragraph(), clear(), paragraph add_line_break(), paragraph run text/add_run()/font formatting/hyperlinks, paragraph alignment/level/spacing/default-font formatting including language_id, word_wrap, auto_size, margins, and vertical-anchor properties while changing only the target slide XML plus slide relationships when hyperlinks change. fit_text() writes conservative fit XML for table cells where python-pptx currently cannot compute _Cell extents. |
| `python_api.table_cell_fill` | P0 | [+] Supported | Read and set table.cell(row, col).fill solid, gradient, pattern, and background fills | python-pptx parity probe + package diff + surface ratchet + benchmark coverage + Open XML validation | The facade mirrors python-pptx table cell fill.solid(), fill.background(), fill.gradient(), gradient_angle, gradient_stops color/position helpers, fill.fore_color RGB/theme colors, and patterned foreground/background RGB/theme colors by editing only the target slide XML cell properties. |
| `python_api.table_cell_merge` | P0 | [+] Supported | Merge and split table cells with span metadata | python-pptx parity probe + package diff + surface ratchet + Open XML validation | The facade mirrors python-pptx table cell merge(), split(), is_merge_origin, is_spanned, span_width, and span_height by editing only table-cell span attributes and text bodies in the target slide XML. |
| `python_api.table_style_flags` | P0 | [+] Supported | Read and set table style flags and iterate cells | python-pptx parity probe + package diff + surface ratchet | The facade mirrors python-pptx table.first_row, first_col, last_row, last_col, horz_banding, vert_banding, iter_cells(), and notify_* no-op hooks while changing only the target slide XML for flag edits. |
| `python_api.add_table` | P0 | [+] Supported | Add a table with slide.shapes.add_table(rows, cols, left, top, width, height) | python-pptx parity probe + semantic diff + package diff | The facade queues table insertion through the native Rust add-table operation and allows cell edits before save. |
| `python_api.add_chart` | P0 | [+] Supported | Add a chart with slide.shapes.add_chart(...) and group.shapes.add_chart(...) | python-pptx parity probe + chart metadata check + package diff + Open XML validation | The facade adds python-pptx-style category charts, XY scatter charts, bubble charts, and 3D bubble charts on slides, top-level group shapes, and nested group shapes by transplanting packaged chart templates, an embedded workbook, slide relationship, and content-type entries into the original package without importing python-pptx at save time. Chart additions accept python-pptx-compatible no-series and empty-series data, plus sparse/mismatched and hierarchical category chart data. Chart graphic frames expose python-pptx-style shape_id values before save and shape.chart_part metadata for read parity. |
| `python_api.chart_inspection` | P0 | [+] Supported | Inspect chart.element, chart.plots, chart.series/points, chart.chart_style, chart fonts, chart formats, axis gridlines, and tick labels | python-pptx parity probe + chart-read benchmark + package diff | The facade mirrors python-pptx chart XML inspection, plot/category/series reads, series index/format/data-label proxies, point format/data-label/marker proxies, point data-label text-frame paragraph/run/hyperlink/fit_text surface, chart_style reads/writes, chart_title.element/has_text_frame metadata, writable chart/axis title formats, writable axis and major-gridline line formats, writable chart font language_id, major gridline proxies, tick-label number-format reads, tick-label font language_id, and category-axis type metadata while isolating style edits to the chart XML part. |
| `python_api.chart_replace_data` | P0 | [+] Supported | Replace chart data with shape.chart.replace_data(CategoryChartData/XyChartData/BubbleChartData) | python-pptx parity probe + package diff + Open XML validation | The facade mirrors python-pptx for simple, date-category, sparse, and hierarchical category data, XY scatter, and bubble chart data replacement plus series-count changes and uneven XY/bubble point counts, while updating both chart metadata reads and the embedded workbook payload. No-series replacement is parity-covered, but zero-plot outputs can fail strict Open XML validation just like python-pptx, so Open XML benchmark evidence uses empty-series and sparse replacement cases. |
| `python_api.chart_title_text` | P0 | [+] Supported | Read and set chart.has_title plus chart.chart_title text-frame, fit_text, rich text, and format fill/line styling | python-pptx parity probe + XML title rich-text check + package diff + Open XML validation | The facade mirrors python-pptx chart title creation/removal, text_frame text, margins, word_wrap, vertical_anchor, auto_size, fit_text() conservative XML, paragraphs.add_paragraph(), paragraph.text, paragraph alignment/level/spacing/default-font formatting including language_id and rich font fill, runs.add_run(), run.text, common run font formatting including language_id/rich font fill, and chart_title.format fill plus patterned line-fill styling while changing only the target chart XML part. |
| `python_api.chart_legend` | P0 | [+] Supported | Read and set chart.has_legend, chart.legend position/layout flags, and legend font formatting | python-pptx parity probe + XML legend check + package diff + benchmark + Open XML validation | The facade mirrors python-pptx chart legend creation/removal, legend position/layout flags, and common legend font formatting including rich gradient fills while changing only the target chart XML part. |
| `python_api.chart_axis_title` | P0 | [+] Supported | Read and set category_axis/value_axis title text-frame flow, fit_text, paragraph formatting, runs, and run font formatting | python-pptx parity probe + XML axis-title rich-text check + package diff + Open XML validation | The facade mirrors python-pptx category/value axis title creation/removal, text_frame text, margins, word_wrap, vertical_anchor, auto_size, fit_text() conservative XML, paragraphs.add_paragraph(), paragraph.text, paragraph alignment/level/spacing/default-font formatting including language_id and rich font fill, runs.add_run(), run.text, and common run font formatting including language_id and rich font fill while changing only the target chart XML part. |
| `python_api.chart_axis_properties` | P0 | [+] Supported | Read and set common category_axis/value_axis visibility, gridline, tick-label, order, scale, unit, and crossing properties | python-pptx parity probe + package diff + benchmark + Open XML validation | The facade mirrors python-pptx for visible, axis line formatting, major-gridline line formatting, major/minor gridline flags, major/minor tick marks, tick-label position, tick-label number format/source linkage/font size/font language_id/category offset, reverse order, min/max scale, value-axis major/minor units, and value-axis crossing controls while changing only the target chart XML part. |
| `python_api.chart_data_labels` | P0 | [+] Supported | Read, add, configure, and remove plot and point data labels | python-pptx parity probe + package diff + benchmark + Open XML validation | The facade mirrors python-pptx plot.has_data_labels and plot.data_labels for value/category/series/percentage/legend-key visibility, label position, number format, source linkage, and rich default font formatting including language_id, gradient fills, and patterned fills, plus series.data_labels and point.data_label default fonts, text-frame toggles, paragraph layout/default rich font fills/line breaks, run font language_id/rich fill, and fit_text() conservative XML while changing only the target chart XML part. |
| `python_api.chart_point_formatting` | P0 | [+] Supported | Set chart series, per-point, and point-marker fill modes/colors, line formatting, marker style, and marker size | python-pptx parity probe + package diff + benchmark + Open XML validation | The facade mirrors python-pptx series/point/point-marker format.fill.solid(), format.fill.background(), format.fill.patterned(), format.fill.gradient(), fill pattern, gradient angle/stops, foreground/background RGB, format.line.fill.solid(), format.line.fill.background(), format.line.fill.patterned(), line fill pattern foreground/background RGB, format.line.fill.fore_color.rgb, format.line.color.rgb, format.line.width, format.line.dash_style, point.marker.style, and point.marker.size for category charts while changing only the target chart XML part. |
| `python_api.chart_plot_properties` | P0 | [+] Supported | Read and set chart plot properties such as vary_by_categories, gap_width, and overlap | python-pptx parity probe + package diff + Open XML validation | The facade mirrors python-pptx plot.vary_by_categories, gap_width, and overlap semantics, including default behavior when plot spacing XML is absent, while changing only the target chart XML part. |

## Rendering oracles

| id | priority | status | capability | oracle | notes |
|---|---:|---|---|---|---|
| `rendering.powerpoint_export` | P2 | [+] Supported | Render through Microsoft PowerPoint as gold oracle | PowerPoint PDF export + PNG conversion | Optional corpus lane stages decks in Office TemporaryItems, disables macros for automation opens, exports PDF through Microsoft PowerPoint, and converts pages to PNGs for the gold visual oracle. |
| `rendering.libreoffice_smoke` | P2 | [+] Supported | Render through LibreOffice as non-authoritative smoke | LibreOffice headless export | Optional corpus lane exports all checked-in fixtures to PDF when LibreOffice is installed; this remains non-authoritative. |
